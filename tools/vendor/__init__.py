"""Staging the parts of the submodules the program actually reads.

Neither submodule is read at run time. These copy the megabyte or so that
matters into the package, where it is committed and shipped.
"""
