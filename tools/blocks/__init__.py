"""The block lookup tables: what shape each family is, and what each face of it
reads.

`geometry.py` is the pieces the generators are written with and `tables.py`
puts their answers into the lookup files; everything else beside them is one
subject's worth of families.
"""
