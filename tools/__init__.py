"""The scripts that build Scaffold's data, and the ones that check it.

Nothing here ships. Every one of them is run from the repository root, as a
module: `python -m tools.blocks.mounted`. `python -m tools.generate` runs
every generator in order, which is what `build.py` does before it freezes
anything.
"""
